{
  "name": "elythria-astro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:content": "node scripts/sync-content.mjs",
    "predev": "npm run sync:content",
    "dev": "astro dev",
    "start": "astro dev",
    "check": "astro check",
    "prebuild": "npm run sync:content",
    "build": "astro build",
    "preview": "astro preview",
    "verify:build": "node scripts/verify-build.mjs",
    "test:e2e": "playwright test",
    "verify": "npm run check && npm run build && npm run verify:build && npm run test:e2e"
  },
  "dependencies": {
    "astro": "^5.13.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@playwright/test": "^1.55.0",
    "typescript": "^5.8.3"
  }
}
